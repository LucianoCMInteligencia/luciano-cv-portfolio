{
  "buildCommand": "npm install -g pnpm --force && pnpm i && pnpm run build",
  "installCommand": "npm install -g pnpm --force && pnpm i",
  "outputDirectory": "dist",
  "nodeVersion": "20.x",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
